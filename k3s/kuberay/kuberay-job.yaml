apiVersion: ray.io/v1
kind: RayJob
metadata:
  name: kuberay-job
  namespace: ray
spec:
  entrypoint: python3 /home/ray/app/repo/k3s/kuberay/main.py

  shutdownAfterJobFinishes: true
  ttlSecondsAfterFinished: 20

  runtimeEnvYAML: |
    env_vars:
      # Final training (single run): fewer workers, more CPU per worker.
      NUM_WORKERS: "2"
      CPUS_PER_WORKER: "8"
      # Tuning stage: lighter per-trial footprint; bounded by laptop RAM.
      NUM_WORKERS_TUNE: "2"
      CPUS_PER_WORKER_TUNE: "2"
      # With ~13GB RAM total, 4GB/trial typically allows 2 concurrent trials safely.
      MAX_CONCURRENT_TRIALS: "2"

  rayClusterSpec:
    rayVersion: "2.52.0"
    enableInTreeAutoscaling: false # true
    autoscalerOptions:
      version: v2
      upscalingMode: Default
      idleTimeoutSeconds: 60

    ########################
    # HEAD
    ########################
    headGroupSpec:
      rayStartParams:
        # Per Ray docs: setting num-cpus:"0" prevents scheduling CPU workloads on head.
        num-cpus: "0"
      template:
        spec:
          containers:
            - name: ray-head
              image: ray-cluster:2.52.0
              ports:
                - containerPort: 6379
                  name: gcs-server
                - containerPort: 8265
                  name: dashboard
                - containerPort: 10001
                  name: client
              resources:
                limits:
                  cpu: "1"
                  memory: "2Gi"
                requests:
                  cpu: "1"
                  memory: "2Gi"
              volumeMounts:
                - name: job-files
                  mountPath: /home/ray/app
              envFrom:
                - secretRef:
                    name: env-secret
              env:
                - name: RAY_enable_infeasible_task_early_exit
                  value: "true"
                - name: RAY_DEFAULT_OBJECT_STORE_MEMORY_PROPORTION
                  value: "0.3"

          initContainers:
            - name: git-sync
              image: registry.k8s.io/git-sync/git-sync:v4.1.0
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
              env:
                - name: GITSYNC_REPO
                  value: "https://github.com/takenking9879/Kubernetes-MLOPS-platform"
                - name: GIT_SYNC_BRANCH
                  value: "main"
                - name: GIT_SYNC_ROOT
                  value: "/git"
                - name: GIT_SYNC_DEST
                  value: "repo"
                - name: GIT_SYNC_WAIT
                  value: "30"
                - name: GITSYNC_ONE_TIME
                  value: "true"
              volumeMounts:
                - name: job-files
                  mountPath: /git

          volumes:
            - name: job-files
              emptyDir: {}

    ########################
    # WORKERS
    ########################
    workerGroupSpecs:
      - replicas: 2
        minReplicas: 2
        maxReplicas: 2
        groupName: small-group
        rayStartParams: {}

        template:
          spec:
            affinity:
              podAntiAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      labelSelector:
                        matchExpressions:
                          - key: ray.io/group
                            operator: In
                            values:
                              - small-group
                      topologyKey: kubernetes.io/hostname

            containers:
              - name: ray-worker
                image: ray-cluster:2.52.0
                resources:
                  limits:
                    # Laptop single-node "simulate 2 nodes": run 2 worker pods on the same host.
                    # Sized so both worker pods can be scheduled together.
                    cpu: "9"
                    memory: "5Gi"
                  requests:
                    cpu: "9"
                    memory: "5Gi"
                volumeMounts:
                  - name: job-files
                    mountPath: /home/ray/app
                envFrom:
                  - secretRef:
                      name: env-secret
                env:
                  - name: RAY_enable_infeasible_task_early_exit
                    value: "true"
                  - name: RAY_DEFAULT_OBJECT_STORE_MEMORY_PROPORTION
                    value: "0.5"

            initContainers:
              - name: git-sync
                image: registry.k8s.io/git-sync/git-sync:v4.1.0
                resources:
                  requests:
                    cpu: 100m
                    memory: 128Mi
                  limits:
                    cpu: 200m
                    memory: 256Mi
                env:
                  - name: GITSYNC_REPO
                    value: "https://github.com/takenking9879/Kubernetes-MLOPS-platform"
                  - name: GIT_SYNC_BRANCH
                    value: "main"
                  - name: GIT_SYNC_ROOT
                    value: "/git"
                  - name: GIT_SYNC_DEST
                    value: "repo"
                  - name: GIT_SYNC_WAIT
                    value: "30"
                  - name: GITSYNC_ONE_TIME
                    value: "true"
                volumeMounts:
                  - name: job-files
                    mountPath: /git

            volumes:
              - name: job-files
                emptyDir: {}
